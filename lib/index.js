'use strict';
exports = module.exports = Suspiro;

function Suspiro(runner) {
  this.attachEvents(runner);
}

Suspiro.prototype.attachEvents = function(runner) {
  runner.on('', function() {
  });
};
